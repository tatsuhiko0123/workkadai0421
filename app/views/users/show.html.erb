<h1><%= @user.name %>のページ</h1>
<p>メールアドレス: <%= @user.email %></p>
<p>お気に入り一覧</p>
<% @favorites.each do |f| %>
  <div>
    <p> 名前 : <%= f.blog.user.name %></p>
    <p> コメント : <%= f.blog.content %></p>
  </div>
<% end %>
<%= link_to '新しくブログを投稿する', new_blog_path %><br>
<%= link_to "ブログ一覧画面にもどる", blogs_path %>
